import React from 'react';
import { Box, Typography } from '@material-ui/core';

export const Footer = () => {
  return (
    <Box>
      <Typography>Footer</Typography>
      <Typography>Footer</Typography>
    </Box>
  )
}
